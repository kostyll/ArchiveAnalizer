main:
  # секция значений создания отчетов
  report:
    in_day: False
    cam_in_day: False
    cam_in_hour: False
  # секция для доступа к кэш-бэкапам
  backup:
    file:
  # прочие триггеры
  triggers:
    exec_after_create: True
    each_create_new_cache: False
  # рабочие директории
  dir:
    app_dir: "Intellect Archive Analizer"  # имя рабочих директорий программы
  # настройка БД
  db:
    base: ":memory:"
    table: "archiv"



